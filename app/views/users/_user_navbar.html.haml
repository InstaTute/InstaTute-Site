%nav.navbar.navbar-light.bg-faded.navbar-fixed-top
  %a.navbar-brand
    = link_to "InstaTute", root_path
  - if user_signed_in?
    = link_to('Logout', destroy_user_session_path, :method => :delete, :class => "btn btn-secondary btn-sm pull-xs-right")
  - else
    = link_to('Login', new_user_session_path, :class => "btn btn-secondary btn-sm pull-xs-right")
  - if user_signed_in?
    = link_to('Edit registration', edit_user_registration_path, :class => "btn btn-secondary btn-sm pull-xs-right")
  - else
    =link_to('Register', new_user_registration_path, :class => "btn btn-secondary btn-sm pull-xs-right")
